## 配图2：三层信息加载（框架图）

## 提示词

扁平矢量框架图，主题：Skills 的三层信息加载。奶油色背景（#F5F0E6），黑色轮廓线，极简构图，大量留白。比例 16:9。
画面中心是一台简化的电脑：CPU、内存、硬盘三块模块。
上方分三层堆叠卡片（从上到下）：
第一层：元数据（名字+描述，启动时加载，100 token/个）
第二层：指令（SKILL.md，触发时加载）
第三层：资源（参考文档/脚本，按需加载）
用箭头指向：硬盘→内存→CPU，强调“渐进式加载、省token、更专注”。
右侧用大字标题：三层信息加载。文字中文手写风格，清晰可读。无渐变、无复杂背景。

